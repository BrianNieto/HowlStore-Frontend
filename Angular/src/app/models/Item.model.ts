export class ItemModel{

  constructor(public idItem:number, public nombreItem:string, public precioItem:number, public stockItem:number, public estadoItem:string, public img1:string, public img2:string, public img3:string, public categoryId:number) {
  }
}
